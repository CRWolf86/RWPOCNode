This app was made using nodejs, express, ejs, body-parser, to install all the dependencies once you are on the RWPOCNODE 
folder on your console type:
npm install
Once everything is installed type node app and go to localhost:3000/index to start using the app. To add a new exercise
click on the "add new workout" button